# Local development environment variables
# Copy to .env when needed and customize values.

APP_ENV=development
APP_PORT=8000
FIRESTORE_PROJECT_ID=store-backend-local
JWT_SECRET=change-me-local
JWT_EXPIRATION_MINUTES=60
